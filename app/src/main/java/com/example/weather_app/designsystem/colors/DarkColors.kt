package com.example.weather_app.designsystem.colors

val DarkColors = YSColors(
    primary = primaryDark,
    gradientBackground = BackgroundColors(
        gradientBackgroundStart = GradientBackgroundDarkStart,
        gradientBackgroundEnd = GradientBackgroundDarkEnd
    ),
    textColors = TextColors(
        titleColor = titleColorDark,
        bodyColor = bodyColorDark,
        hintColor = hintColorDark
    ),
    errorColor = errorColor,
    successColor = successColor,
    warningColor = warningColor,
    primaryIconColor = primaryIconColor
)