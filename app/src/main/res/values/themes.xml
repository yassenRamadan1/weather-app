<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use AppCompat DayNight for compatibility with AppCompatActivity and Compose -->
    <style name="Theme.Weatherapp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Optional: Customize the status bar and navigation bar here if needed,
             though enableEdgeToEdge in Activity is generally preferred. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Weatherapp</item>
    </style>
</resources>